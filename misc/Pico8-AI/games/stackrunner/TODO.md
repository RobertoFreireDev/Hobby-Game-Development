What to do:
- Implement 1 to do item at a time (per Claude code session)
- For each item, review @games/stackrunner/Plan.md
- Udpate this Plan.md in the end
- for any UI changes, keep color palette: 0,5,6,7 and 8 (only for enemies)

TO DO:

- [x] Change movement logic: if play tries to move (4 directions, it ONLY spend the action/card IF player moves: it must not spend movement action/card if it is blocked by wall, closed door, can't move box, etc). Also it doesn't count as a movement. for example. it doesn't count as a player movement for bomb countdown.
- [x] Change all the leves from being hard coded to be placed on mapsheet. all actions (items/movements) and objects (wall, door, etc, stair), initial player position is placed on the mapsheet. so I can manually change each level manually later. adjust the game code to load each level/room. Note: player is placed on mapsheet but of course, only during level play, it must no be drawn (don't create a bug where player is removed on mapsheet and then later when loading room/level, there is no more initial player position)
- [x] When player is caught/pushed by the 2 enemies, I can't see what happened. it is so fast. it must stop (block any movement/action/button pressed from player) and display visually what happened caught/pushed by the 2 enemies first before continue. Example: for caught, it must let the player move to next to enemy (blink the player between current colors and all white for 1 second) before showing modal caught. For pushed, it must let the player move to next to enemy (blink the player between current colors and all white for 1 second) before moving/pushing the player
- [x] Add a intro screen (X -> to play, O -> to see tutorial). Tutorial, (X/O -> go back to intro screen)
- [x] UI adjustment: change color from yellow to white for the blinking current movement action and make the add +1 in x for the rect drawn 
- [x] UI adjustment: change any red color to white. example: the modal that display player doesn't have more movements. the bomb rect markup
- [x] Add music that matches a dungeon and puzzle game. it must be calm/slow/relaxing music since the player needs to think about next moves
- [x] Take advantage of the spritesheet still available. Add more sprites to animate player moving (walking) in 4 directions
- [x] Add color 8 (red) to be used only for the 2 enemies
- [x] Take advantage of the spritesheet still available. Add more variety of grass and walls, and apply pixel-art technique to every 12x12 sprite: top-left light source, selective outline (no black ring, no pillow shading), a value budget that keeps white for the player/pips/stair, clustered pixels, single-pixel highlights, sparing dithering and consistent stair-stepping. Four floor slabs (tiles 40-43) and four wall courses (44-47), hashed per cell. The wall courses share one brick bond and differ only in wear. Hue-shifted shading skipped -- it needs colours outside the locked 0/5/6/7/8 palette.
- [x] If shows modal "no moves left", when clicking x/o button, after close the modal, it goes back in time (undo movement) and if there is no movements lefts (player just started the new level without movement action), go back one level. Don't make the player press undo button or start and go back level if this is the only available option to continue playing
- [x] 3 things for reimplement the levels from scratch: 1 -reimplement the levels from zero, 2 - create .md file documentation at the end in the repo to explain what each level is trying to challenge the player and which elements were used in which combination, order and quantity and 3 - full-chain winnability verification. More details for review level designs: The challenge happens at 2 levels: macro (the levels and which items to leave or bring to next level) and micro (Every button for both movement/item pressed during a level). Each movement/item and the order it was collected and used matters. Make the user think about each button pressed because it will impact. Don't add too much of the same element (wall, item) on the level. Difficult increases over levels. Make combination of puzzle decisions based on all available resources (wall, sokoban with boxes, enemies, bomb, movement, door). keep in mind to implement some tree decisions based on which items user will bring to next level. When a new element (enemy 1, enemy 2, moveable box, switch and door, bomb and movement card) is presented for the first time on game, it must be mandatory to player fail in at least 1 situation around this new element. Levels must not have less than 10 possible paths. At least 2 sucessull paths must exist.
- [x] Remove the enemy that push player. Keep only the one that kills you. Update documentation (done first, so the rebuilt levels are designed against the final piece set)
- [x] Adjustments: 1 - if you don't have undo to do (level just started) undo, go back 1 level. Mark a light grey outline circle on floor for the starting position of each level so the player can differentiate if player wants to go back level or not. 2 - Adjust the move box animation. Right now, it is moving the box before the player is close to the box. It doesnt look the player moved the box. (It should move when the player is one 12x12 tile before the box)
- [x] Create a new cover art label. Player on a bigger size (more realistic) inside a dungeon. don't draw any elements from the spritesheet. Keep the title and use these indexes 0,5,6,7 for the color palette. The finished art also sits in labelgen.p8 own __gfx__ (a 128x128 sheet, exactly the size of the label) so it can be repainted by hand in the pico-8 sprite editor: sheet=true dumps the sheet as-is, sheet=false recomposes it from code
- [x] When going back 1 level, make a 0.5 pause (don't update anything on the game) so the player don't accidentaly press go back 1 level again. The idea is to avoid the player going back 2 or 3 levels back by pressing a little more the undo button by mistake 
