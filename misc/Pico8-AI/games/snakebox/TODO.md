What to do:
- Implement 1 to do item at a time (per Claude code session)
- For each item, review @snake-pico8-guide.md
- Udpate documentation in the end

TO DO:

- [x] Implement core game logic — guide §§1-5 in `game.p8`, placeholder `cell()` art from §6, verified headless (131k assertions, soak + full-board fill)
- [x] Implement music and sfxs — guide §8, generated by `audiogen.js`: 27 sfx (6 pentatonic step degrees, 7 one-shots, 14 bed patterns) + an 8-pattern ~68 s loop, verified headless
- [x] add autodetect end of the game — guide §§1, 5, 12: `dest()`/`fatal()`/`trapped()` share the move rules, checked after every step and after the win test; the panel names which death it was ("no way out" vs "bit yourself"). Verified headless (600 soak runs, incl. a bite-avoiding player that can only lose by being boxed in)
- [x] Review the whole art style to polish game — guide §6: the placeholder `cell()` rects became six generated 2x2 sprites (`spritegen.js`), dither baked in so `fillp` leaves the draw path. A directional head points its eyes where you last moved (`flip_x`/`flip_y` off two tiles), plus brick walls, a recessed door panel that keeps the floor letter legible, and a glowing food core. One global shadow ink (4) and a 14 cut-out key, because a shadow drawn in a piece's own dark colour vanishes into its dither. Verified headless (32k `pget` assertions over all six pieces, both flips, both end panels)
- [x] Add cover art label — guide §6.8, `PICO8-LABEL.md` pipeline: `labelgen.p8` composes the 128x128 cover with plain draw calls (no `spr`, no sprite sheet — every piece redrawn at 10px from the §6.5 colour roles), `label-tool.js` splices the dump into `game.p8`. Three stacked 6x6 floor plates with the body running off the front one onto floor b, a 4x5 block-font title, and the game's own bottom bar. Verified headless (cart runs with the label spliced in) and by exporting the `.p8.png` to confirm PICO-8 reads it
- [x] Fix bugs: 1 - reversal — guide §§1, 5, 12: `dest()` now refuses any landing on the neck (`snake[2]`), tested after the door transit. At length 2 the neck is also the tail, so it vacated and nothing else caught it; a direction-based test was rejected because it also blocks a legal move on the far side of a door. `trapped()` inherits the rule, so a start ringed by 3 walls is now a real dead end and `new_game()` re-rolls it. 2 - food in doorways — doors left the food pool (`free_cells()`) **and** the fill target (`total_cells` is now `108-walls-6`, `filled()` skips door-borne segments), because excluding them from only one is what caused the original endgame deadlock. Bottom bar totals follow: board 93-96, floor 32-33. Verified headless (105k assertions, 60 soak games, a scripted full-board win and a near-full board with 300 spawns)
- [x] Persist max score after game over. Update bottom bar: show max score — guide §§1, 2, 4 (new "saved state"), 5, 9, 12: `cartdata("rfreire_snakebox")` opens slot 0, `best=dget(0)` at boot, `dset` on the bite that beats the record rather than inside `die()` — a run abandoned from the pause menu never reaches `die()`, and the winning bite (`score+=1` runs before the win test) would otherwise be the one ending that never banked its score. The score line gained a right-aligned `best n` column, in 9 while this run owns the record and 6 when it does not, so the bar keeps its three inks instead of taking a fourth; both end panels cover the board only, so it stays readable at game over. Id is the name the game ships under, since a cartdata id is global to the machine and renaming it abandons every save. Verified headless across 4 processes (63 assertions: persistence proven by reloading in a fresh process, a 150-game soak asserting best is the running maximum and always matches disk, the win path, and pget checks on both bar branches at the widest line 1 the game can produce), plus a 3-mutant negative control that confirms those assertions fail when the feature is broken, and 1800 live frames through the real `_update`/`_draw`. Also fixed 4 pre-existing drifts between guide §5 and the cart (head facing, `sfx_win` channel, the factored-out `panel()`) and a stale §10 bullet still claiming food spawns on doors — §5 now matches the cart line for line
- [x] Remove the third line on bottom bar. PLayer doesn't need hint. decrease the height of bottom bar based on removed space and center the board grid game — guide §§2, 5: the bar is now two rows (`bar_h=24`, `bar_y=128-bar_h=104`) and `oy` is derived from what it leaves over (`(bar_y-rows*tile)` = 4), so the board is centred both ways instead of pinned to the top. The pure hint (`arrows move   o/x look`) is gone; the other branch of that row was **state**, not a hint — which floor the head is on — so it moved into row 2's right column as `head a` in 9, built exactly like row 1's `best n`. Widest row 2 is `view  a  32/32` ending at x=60 against `head a` starting at x=98, 38 px of daylight. Also fixed a latent off-by-one this exposed: `draw_board`'s grid loops ran `0..cols`/`0..rows`, drawing a stray colour-2 line at x=112 and y=100, one pixel outside the board rect — the bottom one had only ever been invisible because the bar sat on top of it, and it became a line under the board the moment the board moved up. Interior posts only now; the outer `rect` was already drawing those edges. Verified headless (32 pget assertions over a deterministic worst-case board: a full sweep proving every pixel outside the board and above the bar is bare, nothing at all below row 2, both branches of the head indicator, and the column-collision margin), plus a 6-mutant negative control — old bar height, board back at the top, hint row restored, stray grid lines restored, indicator overrunning the margin, indicator dropped — all six caught, and 120 live frames through the real `_update`/`_draw`. The cover art label keeps its own 15 px bar and hint line: that is artwork, not the game bar, and the next item covers it
- [x] Prevent player bit itself — guide §§1, 5, 6.5, 6.6, 8.9, 10, 12: a landing on a body segment is now refused like a wall instead of killing, with a sound of its own (`sfx_deny`, slot 27 — a descending E-C-A over the Am7/Cmaj7 vamp, pitched and audible where the bump is a near-silent noise thud, so a refusal never reads as a dropped input). The neck keeps the bump: it is refused by `dest()` as terrain and it is the most-repeated failed press in the game. The "no way out" death is untouched — the bite death stays in `try_move()` behind an `if trapped()` guard, which is unreachable (`trapped()` runs after every move and at the start re-roll) and is exactly why it is kept: refusing unconditionally would turn any future hole in `trapped()` from an early death into a **soft lock** where every press is denied and the game never ends. The door sweep also moved below the refusal, so a press that never became a step no longer plays one. Tail is a seventh sprite (13) rather than a recolour: 12x12 inset 2px on every side so the floor shows around it, fill 3 with 11 at 25%. A 50% 3/11 checker was tried first and rejected — it is the body's own 11/3 checker inverted, and at 50% that looks identical. No facing on it: the taper reads as "end" from any direction and a segment has no direction to store. Verified headless (41 assertions: the refusal leaves length, head and state untouched and plays the right slot; tail-entry and edge presses are still not denied; the enclosing move still dies "no way out"; a hand-built trapped position still dies "bit yourself"; a 30k-press reckless soak over 30 games with 396 refusals, zero bite deaths, zero body overlaps and never a denial while trapped; per-pixel compares of head, body and tail against the sheet including the cut-outs showing floor through), plus a 5-mutant negative control — always-die, deny-but-still-move, silent deny, tail drawn as body, and the guard removed — all five caught, and 200 live frames through the real `_update`/`_draw`
- [x] Add mini map for other boards not visible. make each block/tile of the minimap a 4 pixel rectfill (green for snake body, dark grey for wall, blue for door, red for food, purple for available). 4x6 in width/height = 24 for each minimap. Add margin to board game and minimaps. Place board game on the top left corner and 2 mini maps on right side (vertically: the letter, the corresponding mini map of that letter, the other letter and the corresponding minimap of this other letter). Keep consistency in art style. Remove logic to toggle view between maps (not needed anymore).
 — guide §§1, 2 (rewritten, plus a new "Minimaps" and "The bar"), 4, 5, 6.5, 8.8, 8.9, 9, 10, 11, 12: `draw_minimaps()` draws the two floors the board is not showing at 4 px a tile, from `walls`/`doors`/`food`/`snake` every frame — no state of its own. The board left its centring to make room (`ox=2`, `marg=2`, column outline at x=100, blocks 101..124), and the pair is centred against it — 24 px of blocks under an 8 px label row, 48 px apart, spanning y=11..92 inside the board's 4..99. Two colour readings the code depends on: the **door is 12, not its base 13**, because 13 is also the floor's dither colour and a 4 px block of it dissolves; and **body beats door** where a segment rests in a doorway, because an occupied door is genuinely shut (`fatal()` tests the landing cell) and losing the body is losing the map. Free tiles take 2 — the ink the big board draws its own grid lines in, so a minimap reads as that grid seen from further away. The view toggle is gone with everything hanging off it: the `btnp(4)`/`btnp(5)` cycles, `auto_follow`, the `view ~= h.f` movement lock (dead code whose failure mode is "silently refuse every input"), and row 2's `head a` column, which could only ever repeat what `floor a` now says. `view` is a cache of `snake[1].f`. O and X stay **unbound** rather than being reused for restart: a run here is long and there is no clock, so a face button that wipes the board is one stray press from throwing it away. SFX slots 9 and 26 are left as holes, because every music pattern refers to its SFX by number and closing a gap re-scores the bed. Both end panels now derive their bounds from `ox`, so they still cover the board and nothing else and the minimaps stay readable under a finished game. Verified headless (102,794 assertions: a per-pixel compare of both minimaps against an independently recomputed expectation, a full-screen sweep proving every pixel above the bar outside the board and the two minimap interiors is bare, the board's left and right edges at x=2/97, row 2's right half bare bar, O and X held **one at a time** for 20 frames each, a move that is no longer blocked by a disagreeing `view`, both end panels clear of the gutter, and a 40-game / 14k-move soak asserting `view == snake[1].f` after every step with 71 full-frame minimap compares), plus a 10-mutant negative control — slot order, column shift, door ink, door-beats-body, `view` stops following, board re-centred, `head a` restored, minimaps dropped, toggle restored, panel overrunning the column — all ten caught, and 1200 live frames through the real `_update`/`_draw`. Also closed a pre-existing §5 drift (`snake={}` in `new_game`) and added three harness traps to `PICO8-TOOLING.md` that all bit during this pass: a driver stubbing `_draw` steals the name from its own tests, an assertion counter overflows PICO-8's 32767, and two inverse controls cancel if a test presses both in one frame. **Left for the rename item:** `labelgen.p8` line 199 still draws `arrows move   o/x look` on the cover art, which is now a control the game does not have
- [x] Fix 2 bugs. Bug 1 - Add only 1, max 2 walls per level and never in the corner where it could create a corner situation where snake could reaach a available space but it could'nt get out next. if 2 walls, give at least 1 tile of difference in space between them. Bug 2 - snake is able to enter door even if there is a snake piece (body or tail) in door.
 — guide §§1, 2 (the bar), 4, 5, 7.1, 7.2, 7.3, 7.6, 10, 12: **Bug 1** — `wall_min,wall_max=1,2`, and `legal_wall_cells()` builds the pool once: interior tiles minus the four diagonally inside a board corner (`corner_dist>=2`), 12 of 16. The corner rule is the one worth arguing. No interior placement can *strictly* trap a cell — the border ring is guaranteed wall-free, so connectivity was already free (§7.6) — but connectivity is not the property that matters here: a wall at (1,1) drops both (1,0) and (0,1) to one exit each and the corner already had two, making a **forced 3-tile run**, which is where a snake dies for a reason the player reads as the generator's fault. That is the tightest pocket this geometry can produce, and excluding it costs nothing. Two walls keep `cheb>=2` so they never fuse into a bar or a diagonal pinch. The **whole set is re-rolled** rather than picked tile by tile — with n at most 2 the spacing rule is the only one a second pick can fail, and rolling the pair together cannot paint itself into a corner; `add(w,{x=c.x,y=c.y})` copies because `rnd(t)` hands back the table, and two floors rolling the same tile would otherwise share one record. Knock-ons: `total_cells` moved 93-96 → **96-99** and the per-floor line 32/32 → 33/33, so the bar's widest row 1 was re-measured — `score 103  fill 99/99` inked to x=88 against `best 103` from x=90, **one bare column at x=89**, which is a tighter fit than §2 previously claimed and is now written down. Free side effect: §7.6's colour-balance trap shrank — the 3-wall roll that needed a 2:1 split is gone and a 1-wall roll cannot unbalance a floor at all. **Bug 2** — `dest()` now refuses a step into a doorway holding any segment, tested **before** the transit branch while `nf` is still the current floor. This was invisible to every collision test in the game and had to be: the head never comes to rest on the door it steps onto, so `fatal()` only ever sees the far end of the passage, and a body in the *near* doorway was the one place the snake could visibly walk through itself. Two decisions. It is refused as `nil` **plus a flag** rather than plain `nil`, because `dest()`'s existing refusals are terrain (edge, block, neck → `sfx_bump`) and this one is the body saying no (→ `sfx_deny`); keeping the test inside `dest()` is what lets `trapped()` inherit it, and lifting it into `try_move()` just to get the sound right is exactly the drift §12 warns about. And the **tail counts**: allowing it would be *correct* — a tail on a door vacates on the same tick, and a transit can never grow since food never spawns on a door — but it is indistinguishable to the player from walking through the middle of the body, so the rule is "any part of me in a doorway shuts it". Price, documented in §7.3 as the intended one: plug both of a floor's doors and that floor is unreachable until they clear. Not a soft lock — `trapped()` calls the same `dest()`, so a head whose only exits are shut doors dies "no way out" on the move that put it there. Also corrected three things this exposed: the start re-roll's `not trapped()` is now **unreachable** at 1-2 walls (a trapped start needs 3 walls around an interior head, and a border head always keeps an open border neighbour) — kept anyway, with the proof in the comment, because its absence is a turn-1 death and the cap is a tuning constant; §12's boxed-in gotcha, which claimed "three interior walls plus a door" as the cause when the buildable case is two edge doors plus a wall around a border tile; and a pre-existing §5 drift where `sfx_eat` sat on the wrong side of the record-banking block. Verified headless (79,433 assertions: a 3,000-roll wall soak asserting the count, interiority, the corner rule, the door exclusion and the spacing, plus that all 12 legal cells get used and both counts get rolled; 300 `new_game()`s proving `total_cells` spans exactly 96-99; seven hand-built door fixtures — an open doorway still transits, a tail in one is refused, a mid-body segment in one is refused, an edge press next to a door is still a bump and not a deny, an occupied *far* end is still `fatal()`'s refusal, and a move that shuts the last door dies "no way out"; a `pget` measurement of the widest bar row; and a 30-game / 8,924-move soak with 155 shut-door refusals asserting each one left the head, the length and the sound alone and never swept a door), plus a **7-mutant negative control** — old wall range, corner walls allowed, walls allowed to touch, walls allowed on doors, the door-occupancy test removed, the same test moved after the transit, and the refusal sounded as terrain — all seven caught, and 200 live frames through the real `_update`/`_draw`. One of those mutants passed on the first run and that is the fourth harness trap now in `PICO8-TOOLING.md`: the spacing assertion read `>=min_wall_dist`, the very global the mutant edits, so it moved with the mutation and agreed with whatever the cart believed. Assert the number, not the name it is stored under.
- [X] Add outline on 2 letters on top of each minimap. make the tail in both board and minimap pink and keep art style
- [X] Only if snake body piece is on top of door, player can't see the door. Add a 16x16 blue rect outline on top of snake body while there is a body piece on top of door.
- [X] Add outline for both board game and minimaps
- [X] Add simple intro before going to game. keep the current art style 
- [X] Rename game to snakebox. Adjust any documentation, code
- [x] Add/Update cover art label. Don't use spritesheet — guide §6.8, `PICO8-LABEL.md` pipeline: `labelgen.p8` re-runs clean and its 128x128 output is **byte-identical** to the `__label__` already in `game.p8`, so the cover the cart ships is exactly the cover the generator describes — no drift between the two, which is the only thing this item could silently be wrong about. Still no sprite sheet anywhere in it: every piece is redrawn at 10 px from the §6.5 colour roles with plain draw calls, including the pink 14/8 tail inset 1 px on every side, because a cover showing one end of the snake is showing a rope. Verified by splicing the fresh dump back in and running the cart headless (90 live frames through the real `_update`/`_draw`), and by exporting `snakebox.p8.png` and opening it — PICO-8 reads the label, it is not merely tolerating it. A recomposed variant was built and discarded: the stack shifted 16 px left to open a right-hand column for the game's two minimaps, which does mirror the current screen but crowds the plates and costs the depth the offset stack buys; a second variant recut the a/b/c plate labels in the title's own block font at 2 px a cell (legible at 1x where the 4 px `print` font is mush) and needed a `c` glyph the font does not have, since "snakebox" has no c. Both are reverted — the cover is deliberately not a screenshot (§6.8), and the plates already say "three floors" without a minimap repeating it. Also closed three §6.8 drifts this pass exposed, all dating from the rename: the title font is documented at 4 px a cell (it is 3, and 4 is exactly what stopped fitting when `snake` became `snakebox` — 142 px into a 128 px cover), the halo is documented as a fixed 2 px (it is one cell, because at cs=3 a 2 px halo closes the `o`'s counter from both sides and fills it), and the bottom bar is documented as carrying "the controls" — it carries the author, the hint having gone with the view toggle that named it. §6.8 now also says outright that the label's 15-row bar is artwork and is **not** the game's 24-row bar, which is the drift the next person is most likely to reintroduce
